# GUI Agent 评估项目当前进展与开发计划

版本：v2.1
更新日期：2026-07-21
状态：项目进展与唯一可执行计划清单；规范性架构以 `docs/01-技术方案.md` 为准，TaskGraph 优化设计以 `docs/06-GUI_Agent任务分解与规划评估优化技术方案.md` 为依据

## 1. 总体判断

当前项目已经形成一条不依赖 `src/oracle` 的新评估链，并落实了“意图匹配优先、执行证据验证在后”的两阶段 checkpoint 判定设计。

当前成熟度应定义为：功能完整且单元测试稳定的工程基线，而不是已经校准可靠的通用判定器。

主要原因：

- TaskGraph schema、人工标注契约、图生成、兼容投影和 payload 双轨输出已完成，并通过 feature flag 保持旧 `_checkpoints` 主链兼容。
- checkpoint 已完成第一轮通用化，RAG 知识内容已覆盖 `settings` 和淘宝“再来一单/历史订单复购”场景，但仍缺批量命中率评估。
- 重复操作、状态边界和规划失效仍包含人工阈值或固定置信度。
- VLM 调用量约为“动作数 + 命中 checkpoint 数”，批跑成本偏高。
- 已完成批跑，但还没有形成基于人工标注集的正式指标。
- `efficiency`、`trajectory` 等模块虽已实现，但尚未接入新主链。
- 新架构回归测试为 `129 passed`（排除 chromadb 依赖的 decomposer 侧测试）。

## 2. 当前执行链路

```text
原始任务目录
  -> 预处理与动作过滤
  -> LLM+RAG 生成 checkpoints
  -> payload.json + 截图序列
  -> 图片路径转 Base64
  -> AB 相邻页面验证
  -> 视觉/OCR 状态证据
  -> checkpoint 与 agent_purpose 意图匹配
  -> checkpoint 截图执行验证
  -> 状态序列重建
  -> 重复操作判定
  -> 规划失效聚合
  -> baseline_result.json
```

主入口为 `src/evaluator/repeated_baseline.py::run_repeated_baseline`，单跑和批跑由 `start.sh` 接入。

下一代链路将在保持上述 v1 可运行的前提下增加：

```text
TaskGraph
  -> checkpoint 兼容投影
  -> 图感知对齐和 TaskProgress
  -> 多条件执行验证
  -> graph-aware planning evaluation
```

## 3. 子模块进展

| 子模块 | 状态 | 当前能力 | 主要缺口 |
|---|---|---|---|
| 启动与批跑 | 已完成 | `single/batch` 模式，自动读取 `.env` | 无并发、断点续跑、输出级别和调用缓存 |
| 原始数据预处理 | 已完成 | 解析 UTG、clearRes、OCR、actionPurpose，过滤非 GUI 节点 | 输出 `_deduped.json`、`_stategraph.json`，但新基线只消费 `payload.json` |
| 终止节点治理 | 已完成 | 过滤原始冗余 finished，只补一个终止哨兵 | 需要用真实数据继续检查提前终止和无终止场景 |
| LLM+RAG 拆解 | 已完成粒度优化 | 生成关键可观察子状态，支持空结果重试、质量校验和一次自动修正 | 输出仍为扁平 checkpoint；知识内容主要为 settings；缺少 TaskGraph 和人工指标 |
| TaskGraph schema | P0-A/P0-B 已完成 | TaskGraph/人工标注模型、严格 JSON codec、DAG 与引用校验、远程证据隐私约束、Decomposer 图生成与兼容迁移 | 图感知 verifier、TaskProgress 和人工指标尚未接入主链 |
| Payload 构建 | P0-B 已完成 | 输出 `_checkpoints`、`_task_graph`、`_task_graph_schema_version`、`agent_purposes`、`_ocr_pages`、`_interruption_events`、源步骤索引 | 仍需统一 benchmark/评估侧对新字段的消费 |
| 图片解析 | 已完成 | 路径统一转 Base64，保留原始图片引用 | 缺少图片哈希、去重缓存和损坏图片统计 |
| ABValidator | 已完成 | 对每个非终止相邻步骤调用 VLM 判断动作效果 | 几乎每步一次 VLM；页面描述存在误差传播风险 |
| 视觉证据 | 已完成 | pHash、SSIM、pixel diff、变化区域、OCR/rawPage 变化 | 权重为人工设定，尚未通过标注数据校准 |
| 子状态聚合 | 第一版完成 | 根据视觉变化、页面描述、导航动作和 checkpoint 进展划分状态 | 主链前后构建两次，视觉指标也被重复计算 |
| 意图匹配 | 已完成 | LLM 比较 checkpoint 与 agent purpose，支持连续 purpose span 和顺序约束 | 不理解依赖、合法换序和替代分支；LLM confidence 未校准 |
| 步骤对齐 | 已完成 | 将 checkpoint 对齐到真实 source step，而非均匀分配 | 一对多、多对一、隐式完成和多次尝试仍需真实数据验证 |
| CheckpointVerifier | 已完成 | 对意图召回候选使用对应前后截图和 VLM 验证 | 每个命中 checkpoint 一次 VLM；缺少多 criterion、`partial` 和缓存 |
| 重复操作判定 | 第一版完成 | 连续重复、等待重复、滑动重复、状态动作循环 | 阈值均为经验值，可靠性尚不能只凭单元测试证明 |
| 规划失效聚合 | 第一版完成 | 缺失检查点、执行阻塞、提前终止、未及时终止 | 未实际使用 TaskGraph/state sequence；部分置信度固定；首错归因不足 |
| 1.2 七类异常 taxonomy | 待补齐 | 顶层分类固定为循环/死循环、重复动作、Grounding 错误、规划失效、Hallucination、异常中断响应、提前终止；已有 repeated/planning/clarify/state-interruption 到 `anomaly_events` 的第一版投影 | Grounding、Hallucination 仍缺可验证判定链；异常中断响应关键词扫描需要标注集校准 |
| 效率评估 | 独立实现 | 无效动作、探索开销、导航冗余、滑动效率 | 接口仍使用 `darwin_result`，未接入新基线 |
| 轨迹差分 | 独立实现 | `no_impact/remedial/cascading` 三分类 | 仍依赖 Darwin 形式的数据，未接入主入口 |
| Benchmark | P0-C 骨架已完成 | 离线读取 annotation、v1 baseline、v2 planning evaluation、legacy 对照，输出 run metadata 和最小汇总指标 | 尚未接入真实标注集 A/B 和正式准入阈值 |
| Legacy Oracle | 隔离完成 | 只用于旧链对比和提示词参考 | 需要进一步隔离 pytest 和重复实现 |
| 输出系统 | 可用但冗余 | 每任务最多输出 10 个 JSON，批跑另有汇总；新增 `anomaly_events.json` | `baseline_result.json` 已包含绝大多数子报告 |
| Web 服务器与面板 | 第一版完成 | FastAPI + SSE 实时日志，面板支持配置触发预处理/评估/全链路运行，Overview/Tasks 双标签结果可视化 | 批量运行进度依赖 subprocess `print()`；无断点续跑和用户认证 |
| 文档 | 已形成分层 | `docs/01-技术方案.md` 为规范文档，优化方案提供 TaskGraph 设计 | 执行计划需要持续与实现和 benchmark 结果同步 |

## 4. 关键代码依据

- 预处理动作过滤：`src/preprocessor/preprocessor.py::_is_non_executable_action` 和 `preprocess`。
- 单一终止哨兵：`src/preprocessor/write_payload.py::write_payload`。
- RAG App 范围配置：`src/preprocessor/pipeline.py::_run_decomposer` 支持 `RAG_APP_NAME`，不设置时不限定知识域。
- 拆解粒度约束与自动修正：`src/decomposer/decomposer.py::_DECOMPOSE_PROMPT`、`_normalize_and_validate` 和 `_finalize_checkpoints`。
- TaskGraph 与人工标注模型：`src/decomposer/models.py`。
- 严格解析、序列化和确定性校验：`src/decomposer/schema.py`。
- 标注契约与远程数据映射：`docs/07-人工标注契约与TaskGraph-Schema.md`。
- 图片路径水合：`src/common/image_resolver.py::hydrate_payload_images`。
- 相邻页面验证：`src/common/ab_validator.py::ABValidator.validate_payload`。
- 意图召回：`src/verifier/alignment.py::match_checkpoint_intents`。
- checkpoint 图片上下文：`src/verifier/alignment.py::build_checkpoint_step_data`。
- checkpoint 执行验证：`src/verifier/verifier.py::CheckpointVerifier`。
- 视觉边界计算：`src/evaluator/visual_evidence.py::_score_boundary`。
- 子状态聚合：`src/evaluator/state_evidence.py::build_state_sequence`。
- 重复操作判定：`src/common/repeated_action_detector.py::RepeatedActionDetector`。
- 错率聚合：`src/evaluator/planning_failure.py::PlanningFailureAggregator`。
- 批跑与报告写出：`src/evaluator/repeated_baseline.py::run_repeated_baseline_batch`。
- Web 服务器：`src/server/server.py` — FastAPI + SSE 流式日志 + 全链路 API。
- Web 面板：`src/server/static/index.html` — 配置触发 + Overview/Tasks 双标签结果可视化。

## 5. 当前模型调用成本

设有效动作数为 `N`，命中 checkpoint 数为 `K`：

```text
预处理拆解：通常 1 次 LLM，空结果或质量修正时增加 1 次
意图匹配：每任务约 1 次 LLM
ABValidator：约 N 次 VLM
CheckpointVerifier：约 K 次 VLM
总模型调用：约 2 次 LLM + N + K 次 VLM
```

失败重试会进一步放大请求数。长轨迹批跑的主要成本来自 ABValidator，而不是意图匹配。

## 6. 执行原则与状态标记

- `[x]`：代码和验证已经完成。
- `[ ]`：当前可执行或满足前置条件后可执行。
- P0/P1/P2 表示交付顺序，不表示所有同级任务必须串行。
- 任何改变判定语义、证据数量或默认输出的优化，必须经过固定标注集 A/B。
- `Subtask` 是 TaskGraph 中的规划语义单元；`Checkpoint` 是现有对齐和验证链使用的兼容投影，二者不得混用。
- 未建立人工指标前，不删除 `_checkpoints`、旧 payload 支持或现有 planning subtype。

## 7. TODO 清单

### P0-A：人工标注契约与 TaskGraph schema

目标：先定义可标注、可验证、可迁移的数据模型。

- [x] 定义人工标注 schema，至少包含 task/app、goal、TaskGraph、subtask span、完成状态、首错步骤、恢复结果和证据引用。
- [x] 新增 `src/decomposer/models.py`，定义 Goal、Subtask、Dependency、AlternativeGroup、Constraint 和 VerificationCriterion。
- [x] 新增 `src/decomposer/schema.py`，定义 `task_graph.v1` 的解析、序列化和版本校验。
- [x] 实现 TaskGraph DAG 校验：环、未知依赖、重复节点、无法求值的 required Subtask 和非法 alternative group。
- [x] 校验 required Subtask 的成功条件可观察且可用于后续验证。
- [x] 固化 3-8 个语义 Subtask 的默认复杂度约束，禁止退化为动作级步骤列表。

验收条件：

- TaskGraph 合法样例可以稳定 round-trip。
- DAG 非法、依赖缺失和不可验证终态可以被本地校验发现。
- schema 变更必须有显式版本，不允许静默改变字段含义。

完成证据：

- `src/decomposer/test_schema.py` 使用纯合成 fixture 覆盖 13 个 schema 场景。
- 真实轨迹、截图、UI 树、日志和服务器路径均未进入本地仓库。
- 新架构完整回归：`129 passed`。

### P0-B：TaskGraph 生成、兼容投影与双轨输出

目标：引入新规划模型，同时保持当前 v1 主链和旧 payload 可运行。

- [x] 在 Decomposer 中实现 `decompose_graph()`，采用“结构生成 + 确定性校验 + 最多一次修正”。
- [x] 实现 `project_checkpoints(task_graph)`，将 Subtask 投影为现有 Checkpoint。
- [x] 实现旧 `_checkpoints` 到 TaskGraph 的兼容迁移函数。
- [x] 在 `payload.json` 中写入 `_task_graph`、`_task_graph_schema_version` 和现有 `_checkpoints`。
- [x] 增加 `TASK_GRAPH_ENABLED` feature flag，迁移期支持 v1/v2 双轨运行。
- [x] 保持 `step_level_instruction`、现有 checkpoint 接口和历史 payload 兼容。
- [x] 增加线性任务、合法换序、替代分支、非法依赖和旧 payload 迁移测试。

验收条件：

- 当前新架构 `135` 个测试全部通过，新增测试通过。
- 老 payload 可以继续运行且结果不因仅增加 `_task_graph` 而变化。
- 新 payload 同时包含 `_task_graph` 和 `_checkpoints`。
- 同一任务的 TaskGraph 投影结果在重复运行中保持稳定。

完成证据：

- `src/decomposer/decomposer.py` 新增 `decompose_graph()`，失败时最多自动修正一次。
- `src/decomposer/projection.py` 新增 TaskGraph 到 checkpoint 的兼容投影和旧 checkpoint 迁移。
- `src/preprocessor/pipeline.py` 通过 `TASK_GRAPH_ENABLED=1` 开启图生成；图无效时回退到旧拆解并迁移为兼容 TaskGraph。
- `src/preprocessor/write_payload.py` 双轨写出 `_task_graph`、`_task_graph_schema_version`、`_checkpoints` 和原有 `step_level_instruction`。
- 新增 `src/decomposer/test_projection.py`、`src/decomposer/test_decomposer_unit.py`、`src/preprocessor/test_task_graph_pipeline.py` 覆盖核心兼容场景。

### P0-C：Benchmark 骨架与 v1/v2 基础对比

目标：让人工标注、v1 checkpoint 和 v2 TaskGraph 可以由同一工具读取和比较。

- [x] 新增 `src/evaluator/benchmark.py`。
- [x] 统一读取人工标注、v1 baseline、v2 planning evaluation 和 legacy Oracle 对照结果。
- [x] 为规则基线、模型版本和 feature flag 记录统一 run metadata。
- [x] 输出最小指标：样本数、解析失败数、schema 不兼容数、v1/v2 结果差异和模型调用统计。
- [ ] 固化当前 v1 结果、配置和提交号，作为后续 A/B 基线。

验收条件：

- benchmark 可以在无真实模型调用的 fixture 上完成端到端运行。
- 同一任务可以并列展示人工标注、v1、v2 和 legacy 对照结果。
- 报告能够追溯代码版本、schema 版本、模型版本和运行参数。

完成证据：

- `src/evaluator/benchmark.py` 提供 `run_benchmark()` 和 CLI，默认只读取本地产物，不调用模型。
- `src/evaluator/test_benchmark.py` 覆盖完整 fixture、JSON 解析失败和 schema 不兼容统计。
- P0-C 当前完成的是 benchmark 骨架；正式 v1 基线固化需要在真实远程样本可用后执行。

### 已完成基础：checkpoint 通用化

- [x] 移除 `app_name="settings"` 固定值，默认不限定 App，并支持 `RAG_APP_NAME`。
- [x] 将提示词改成通用 GUI Agent 任务。
- [x] 规定一个 checkpoint 对应一个路径无关的关键可观察状态边界。
- [x] 检测纯操作名称、复合阶段、缺失验证状态和重复 checkpoint，并自动修正一次。

### P1-A：跨 App 人工评测闭环

目标：先建立定量依据，再扩大默认判定能力。

- [ ] 选择约 30 条跨 App 轨迹进行试标，覆盖不同任务长度、成功/失败轨迹，以及 1.2 定义的 7 类异常：循环/死循环、重复动作、Grounding 错误、规划失效、Hallucination、异常中断响应、提前终止。
- [ ] 根据试标结果修订标注字段、边界案例和标注说明，明确 recovery、impact、efficiency、risk 和 evidence quality 是事件属性或报告维度，不作为顶层异常分类。
- [ ] 将标注集扩展到 100-300 条跨 App 样本。
- [ ] 标注 TaskGraph、Subtask span、achieved/partial/failed/uncertain、首错步骤、恢复结果、异常顶层标签、subtype 和证据引用。
- [ ] 输出 Decomposer 指标：Goal Accuracy、Required Subtask Recall、Subtask Precision、Dependency F1、Constraint Recall、Verifiability Rate、Redundancy Rate。
- [ ] 输出 Alignment/Verification 指标：Attempt Recall、Span IoU/Boundary Error、Status Macro-F1、Forbidden-state Precision、Uncertain Calibration。
- [ ] 输出 Planning/Recovery 指标：Planning Failure Macro-F1、First-error Accuracy、First-error Step MAE、Recovery Detection F1、Cascading Detection F1。
- [ ] 按 App、任务长度、异常顶层类型、subtype、截图/OCR 缺失情况分桶。
- [ ] 对同一标注集比较 v1、v2、legacy Oracle、纯规则基线和不同 LLM/VLM 版本。
- [ ] 设定新能力进入默认主链的最低指标和允许回归范围。

### P1-B：TaskProgress、图感知对齐与规划失效

前置条件：P0 完成，30 条试标已验证标注 schema 可用。

- [ ] 新增 `src/evaluator/task_progress.py`，输出逐步骤 `TaskProgressSnapshot`。
- [ ] 识别 active、available、blocked、achieved Subtask。
- [ ] 新增 `SubtaskAlignment`，支持一对多、多对一、多次尝试和最终成功选择。
- [ ] 支持 allowed reorder 和 alternative branch，合法路径不得因顺序差异被误判。
- [ ] verifier 支持多 criterion 聚合以及 achieved/partial/failed/uncertain。
- [ ] planning failure 同时消费 TaskGraph、TaskProgress、state sequence、alignment 和 verification。
- [ ] 实现 dependency violation、wrong branch、goal misunderstanding 和 replanning failure。
- [ ] 区分 not attempted、attempted but blocked、wrong state 和 insufficient evidence。
- [ ] 输出 `planning_evaluation.json`，定位首个违规 Subtask 和真实步骤。
- [ ] 合并或废弃重复的 `src/common/planning_failure_detector.py`，Legacy Oracle 不再承载新实现。

验收条件：

- 合法换序任务不会被误判。
- alternative group 只要求一条合法路径达成。
- 依赖违反能够定位首个违规 Subtask 和步骤。
- benchmark 证明新实现达到准入指标，且不超过允许回归范围。

### P1-C：按 1.2 七类异常补齐 taxonomy 与事件输出

目标：把异常判定从“重复操作 + 规划失效”扩展为与 `docs/GUI_Agent_异常Case_技术洞察.md` 1.2 一致的 7 类顶层 case。

- [x] 循环/死循环第一版：将 repeated detector 的 `state_action_loop` 投影为顶层 `loop` 事件。
- [x] 重复动作第一版：将 repeated detector 的 ranges 投影为顶层 `repeated_action` 事件。
- [x] 规划失效第一版：将 planning failure events 投影为顶层 `planning_failure` 事件。
- [x] 提前终止第一版：将 planning failure 的 `premature_termination` 子类型投影为顶层 `premature_termination` 事件。
- [ ] 规划失效后续：接入 TaskGraph/TaskProgress，细分 dependency violation、wrong branch、goal misunderstanding 和 replanning failure。
- [x] Grounding 错误第一版：`src/evaluator/grounding.py` — 结合 AB 验证、视觉变化和 OCR 证据，识别 `wrong_tap_target`、`wrong_input_location`、`wrong_scroll_direction` 三种子类型。
- [x] Hallucination 第一版：`src/evaluator/hallucination.py` — 对比 agent 意图与 OCR/页面描述，识别 `non_existent_element`、`wrong_page_understanding`、`fabricated_capability` 三种子类型。
- [x] 异常中断响应第一步：`clarify` 从主执行轨迹剥离，写入 `_interruption_events`，并映射为 `abnormal_interruption_response/manual_clarification_required`。
- [x] 异常中断响应关键词扫描第一版：基于 `state_sequence` 描述识别验证码/安全验证、登录、权限弹窗、Crash/App 异常、网络/加载异常。
- [ ] 异常中断响应后续：识别中断后的等待、重试、放弃、绕路和错误继续，并用标注集校准关键词误报。
- [x] 建立第一版 `anomaly_events` 输出，保留 `category`、`subtype`、`first_error_step`、`related_subtask_id`、`evidence_refs`、`recovery_outcome`、`impact` 和 `confidence`。

说明：执行阻塞、恢复失败、错误级联、效率、证据完整度和高风险操作不作为 1.2 顶层异常分类；它们分别作为 subtype、recovery outcome、impact、report metric 或 risk attribute 进入统一事件结构。

### P1-D：VLM 成本与输出治理

该轨道可与 P1-A/P1-B/P1-C 并行，但改变证据链的优化必须通过固定集 A/B。

可直接实施的无语义优化：

- [ ] 统计每任务 LLM/VLM 调用数、token、延迟、重试和估算成本。
- [ ] 为图片计算内容哈希，并记录图片缺失、损坏和重复率。
- [ ] 建立共享模型缓存，key 包含模型版本、提示词版本、图片哈希和动作/Checkpoint 文本。
- [ ] 复用第一次构建的视觉证据，避免两次完整调用 `build_state_sequence`。
- [ ] 增加 `--output-level compact|full|debug`，批跑默认使用 compact。
- [ ] compact 只写 `baseline_result.json`、必要引用和批跑汇总。
- [ ] 增加批跑断点续跑，仅跳过 schema、代码和模型版本一致的完整任务。
- [ ] 将输出模式、缓存、断点续跑和调用预算参数接入 `start.sh`。

需要 benchmark 后实施的判定裁剪：

- [ ] 增加 `--ab-mode full|needed|skip`。
- [ ] 仅在客观证据充分且 A/B 不降质时减少 ABValidator 调用。
- [ ] 合并可共享截图上下文的 CheckpointVerifier 请求。
- [ ] 为语义裁剪设置自动回退到 full 模式的门槛。

### P1-E：判定阈值与置信度校准

- [ ] 使用标注集校准视觉边界权重和状态切分阈值。
- [ ] 校准重复动作的坐标、文本、页面相似度和连续次数阈值。
- [ ] 对 VLM confidence 做分桶准确率、可靠性曲线和阈值敏感性分析。
- [ ] 用数据替换规划失效中的固定置信度。
- [ ] 分别报告规则证据、模型证据和最终融合置信度。
- [ ] 验证不同 App、任务类型和轨迹长度下的稳定性。

### P1-F：Experience Bank 与可度量 RAG

- [ ] 定义 Decomposition Experience Bank schema。
- [ ] 保存 generated/corrected TaskGraph 对、来源、适用 App/版本/语言和人工审核状态。
- [ ] 建立 candidate、human_verified、production 生命周期。
- [ ] 从人工确认的首错样本提取候选规则和任务模板。
- [ ] 对 Experience Bank 检索前后运行固定标注集 A/B。
- [ ] 根据实际样本缺口扩展多 App Markdown 知识，不以文档数量作为完成标准。
- [ ] 接通并测试 `RAG_PERSIST_DIR`，补齐知识构建、发布和更新流程。
- [ ] 统计 RAG 命中率、错误引用率及其对 TaskGraph 指标的净提升。

### P2-A：恢复、错误级联与轨迹差分

- [ ] 新增 `src/evaluator/recovery.py`。
- [ ] 识别错误后重定位、返回、替代路径和重新尝试。
- [ ] 将 `src/trajectory/differential_judger.py` 改为消费 TaskGraph、TaskProgress 和新链证据。
- [ ] 统一输出 `no_deviation/remedial/cascading`。
- [ ] 增加恢复成本、恢复时延和恢复成功率。
- [ ] 覆盖“走错后恢复成功”“持续错误探索”“首错后级联”等场景测试。

### P2-B：效率、体验与统一报告

- [ ] 将 `src/efficiency/analyzer.py` 的 `darwin_result` 改为新链输入。
- [ ] 在 `baseline_result.json` 增加 `efficiency_prediction`、`trajectory_deviation` 和 recovery 摘要。
- [ ] 统一报告任务完成能力、执行合理性、规划异常、效率、路径冗余、恢复能力、体验和证据完整度。
- [ ] 保证综合评分不能掩盖 required Subtask 未完成或高风险约束违反。

### P2-C：条件式知识图与有限世界模型增强

仅在 benchmark 证明普通 TaskGraph、Experience Bank 和页面证据不足时启动：

- [ ] 将高频 App Markdown 知识转换为状态/转换/技能图。
- [ ] 使用真实轨迹补全合法替代路径。
- [ ] 对提交、删除、支付等不可逆操作增加有限的 next-state 风险判断。
- [ ] 通过固定集 A/B 证明新增结构带来净收益，并记录额外成本。

### 持续工程治理

- [ ] 为 legacy Oracle 测试增加 pytest marker，默认测试不收集资源型 quick test。
- [ ] 增加可选真实 VLM 服务集成测试，默认单元测试继续使用 mock。
- [ ] 为 payload、baseline、planning evaluation 和 batch result 增加统一 schema version 校验。
- [ ] 精简预处理输出策略，明确 `_deduped.json`、`_stategraph.json` 的保留级别。
- [ ] 每轮架构变更同步更新 `docs/01-技术方案.md` 和相关专题文档。
- [ ] CI 中分别运行快速单元测试、离线场景测试和可选真实模型测试。

## 8. P3 延后方向与准入条件

以下工作不属于当前可领取 TODO：

- corrected TaskGraph SFT。
- 分解优劣对的 DPO/Preference Learning。
- 独立 decomposition reward model。
- Coordinator/State Tracker 联合训练。
- execution-feedback RL。
- 独立完整 GUI world model。

只有同时满足以下条件后，才重新评审是否进入活跃 backlog：

1. 100-300 条 TaskGraph 人工标注已稳定。
2. verifier 和 planning evaluation 已完成置信度校准。
3. Experience Bank A/B 已证明检索收益。
4. 存在稳定、可复现的在线执行环境或确定性环境 reward。
5. 训练奖励不依赖单一 Darwin/VLM 结果，且 Executor 独立版本化。

## 9. 当前验证基线

新架构相关测试：

```text
129 passed
```

验证命令：

```bash
python -m pytest src/preprocessor src/decomposer src/common src/verifier src/evaluator src/efficiency src/trajectory -q
```

当前能够证明工程实现稳定，不能证明真实场景准确率达到生产要求。真实 LLM/VLM、跨 App 指标和阈值校准仍依赖 P1 人工评测闭环。

## 10. 推荐实施顺序

```text
人工标注 schema
  -> TaskGraph schema 与本地校验
  -> checkpoint 兼容投影与双轨输出
  -> benchmark 骨架和 v1/v2 基线
  -> 30 条跨 App 试标
  -> 100-300 条标注集与准入指标
  -> 1.2 七类异常 taxonomy 与统一事件输出
  -> TaskProgress 和图感知 alignment
  -> 多条件 verifier 与 graph-aware planning failure
  -> 阈值/置信度校准
  -> Experience Bank 与可度量 RAG
  -> recovery/cascading、效率与统一报告
  -> 有数据依据的知识图或有限 world-model 增强
```

VLM 缓存、证据复用、输出分级和断点续跑可在 P1 并行推进；任何减少证据或改变默认判定的优化必须在固定标注集上验证后启用。
