# GUI Agent 评估项目当前进展与开发计划

版本：v1.0  
更新日期：2026-07-17  
状态：项目进展与执行计划清单；规范性架构以 `docs/01-技术方案.md` 为准

## 1. 总体判断

当前项目已经形成一条不依赖 `src/oracle` 的新评估链，并落实了“意图匹配优先、执行证据验证在后”的两阶段 checkpoint 判定设计。

当前成熟度应定义为：功能完整的工程基线，而不是已经校准可靠的通用判定器。

主要原因：

- 重复操作、状态边界和规划失效仍包含较多人工阈值。
- checkpoint 生成仍存在 HarmonyOS 和 `settings` App 硬编码。
- VLM 调用量约为“动作数 + 命中 checkpoint 数”，批跑成本偏高。
- 已完成批跑，但还没有形成基于人工标注集的正式指标。
- `efficiency`、`trajectory` 等模块虽已实现，但尚未接入新主链。

## 2. 当前执行链路

```text
原始任务目录
  -> 预处理与动作过滤
  -> LLM+RAG 生成 checkpoints
  -> payload.json + 截图序列
  -> 图片路径转 Base64
  -> AB 相邻页面验证
  -> 视觉/OCR 状态证据
  -> checkpoint 与 agent_purpose 意图匹配
  -> checkpoint 截图执行验证
  -> 状态序列重建
  -> 重复操作判定
  -> 规划失效聚合
  -> baseline_result.json
```

主入口为 `src/evaluator/repeated_baseline.py::run_repeated_baseline`，单跑和批跑由 `start.sh` 接入。

## 3. 子模块进展

| 子模块 | 状态 | 当前能力 | 主要缺口 |
|---|---|---|---|
| 启动与批跑 | 已完成 | `single/batch` 模式，自动读取 `.env` | 无并发、断点续跑、输出级别和调用缓存 |
| 原始数据预处理 | 已完成 | 解析 UTG、clearRes、OCR、actionPurpose，过滤非 GUI 节点 | 输出 `_deduped.json`、`_stategraph.json`，但新基线只消费 `payload.json` |
| 终止节点治理 | 已完成 | 过滤原始冗余 finished，只补一个终止哨兵 | 需要用真实数据继续检查提前终止和无终止场景 |
| LLM+RAG 拆解 | 部分完成 | 生成结构化 `_checkpoints`，空结果自动重试 | App 固定为 `settings`，提示词固定为鸿蒙手机，checkpoint 原子性未严格约束 |
| Payload 构建 | 已完成 | 输出 `_checkpoints`、`agent_purposes`、`_ocr_pages`、源步骤索引 | 缺少正式 schema 校验和版本迁移机制 |
| 图片解析 | 已完成 | 路径统一转 Base64，保留原始图片引用 | 缺少图片哈希、去重缓存和损坏图片统计 |
| ABValidator | 已完成 | 对每个非终止相邻步骤调用 VLM 判断动作效果 | 几乎每步一次 VLM；页面描述存在误差传播风险 |
| 视觉证据 | 已完成 | pHash、SSIM、pixel diff、变化区域、OCR/rawPage 变化 | 权重为人工设定，尚未通过标注数据校准 |
| 子状态聚合 | 第一版完成 | 根据视觉变化、页面描述、导航动作和 checkpoint 进展划分状态 | 主链前后构建两次，视觉指标也被重复计算 |
| 意图匹配 | 已完成 | LLM 比较 checkpoint 与 agent purpose，支持连续 purpose span 和顺序约束 | LLM confidence 是模型自评；规则回退需要评估误召回 |
| 步骤对齐 | 已完成 | 将 checkpoint 对齐到真实 source step，而非均匀分配 | 一对多、多对一和隐式完成仍需真实数据验证 |
| CheckpointVerifier | 已完成 | 对意图召回候选使用对应前后截图和 VLM 验证 | 每个命中 checkpoint 一次 VLM，没有缓存或跨 checkpoint 共享判断 |
| 重复操作判定 | 第一版完成 | 连续重复、等待重复、滑动重复、状态动作循环 | 阈值均为经验值，可靠性尚不能只凭单元测试证明 |
| 规划失效聚合 | 第一版完成 | 缺失检查点、执行阻塞、提前终止、未及时终止 | 当前未实际使用 `state_sequence`；部分置信度为固定常数；目标/计划不一致未实现 |
| 效率评估 | 独立实现 | 无效动作、探索开销、导航冗余、滑动效率 | 接口仍使用 `darwin_result`，未接入新基线 |
| 轨迹差分 | 独立实现 | `no_impact/remedial/cascading` 三分类 | 仍依赖 Darwin 形式的数据，未接入主入口 |
| Legacy Oracle | 隔离完成 | 只用于旧链对比和提示词参考 | 需要进一步隔离 pytest 和重复实现 |
| 输出系统 | 可用但冗余 | 每任务最多输出 9 个 JSON，批跑另有汇总 | `baseline_result.json` 已包含绝大多数子报告 |
| 文档 | 基本同步 | `docs/01-技术方案.md` 为规范文档 | 尚未反映成本治理、实验模块接入顺序和评测工具设计 |

## 4. 关键代码依据

- 预处理动作过滤：`src/preprocessor/preprocessor.py::_is_non_executable_action` 和 `preprocess`。
- 单一终止哨兵：`src/preprocessor/write_payload.py::write_payload`。
- RAG 当前固定 App：`src/preprocessor/pipeline.py::_run_decomposer` 中的 `app_name="settings"`。
- 拆解提示词平台限制：`src/decomposer/decomposer.py::_DECOMPOSE_PROMPT`。
- 图片路径水合：`src/common/image_resolver.py::hydrate_payload_images`。
- 相邻页面验证：`src/common/ab_validator.py::ABValidator.validate_payload`。
- 意图召回：`src/verifier/alignment.py::match_checkpoint_intents`。
- checkpoint 图片上下文：`src/verifier/alignment.py::build_checkpoint_step_data`。
- checkpoint 执行验证：`src/verifier/verifier.py::CheckpointVerifier`。
- 视觉边界计算：`src/evaluator/visual_evidence.py::_score_boundary`。
- 子状态聚合：`src/evaluator/state_evidence.py::build_state_sequence`。
- 重复操作判定：`src/common/repeated_action_detector.py::RepeatedActionDetector`。
- 规划失效聚合：`src/evaluator/planning_failure.py::PlanningFailureAggregator`。
- 批跑与报告写出：`src/evaluator/repeated_baseline.py::run_repeated_baseline_batch`。

## 5. 当前模型调用成本

设有效动作数为 `N`，命中 checkpoint 数为 `K`：

```text
预处理拆解：通常 1 次 LLM，空结果重试时 2 次
意图匹配：每任务约 1 次 LLM
ABValidator：约 N 次 VLM
CheckpointVerifier：约 K 次 VLM
总模型调用：约 2 次 LLM + N + K 次 VLM
```

失败重试会进一步放大请求数。长轨迹批跑的主要成本来自 ABValidator，而不是意图匹配。

## 6. 下一步计划

### P0：建立可量化评测闭环

- [ ] 新增 `src/evaluator/benchmark.py`，统一读取人工标注集。
- [ ] 对同一数据运行旧 oracle 和新基线。
- [ ] 输出 intent recall、checkpoint precision/recall、重复操作 precision/recall、规划失效 subtype 指标。
- [ ] 统计图片缺失率、OCR 缺失率、VLM 不确定率、单任务模型调用数和耗时。
- [ ] 固化当前版本结果，作为后续优化对照基线。

这是当前应最先实施的工作。没有指标前继续增加规则或异常类型，会扩大无法校准的判定面。

### P0：通用化 checkpoint 拆解

- [ ] 移除 `app_name="settings"` 固定值，支持从任务元数据解析或不限定 App 检索。
- [ ] 将提示词从“鸿蒙设置操作”改成通用 GUI Agent 任务。
- [ ] 规定一个 checkpoint 对应一个可观察状态边界。
- [ ] 禁止使用“并、然后、同时”生成复合 checkpoint。
- [ ] 增加跨 App、无 RAG、RAG 无命中和复杂指令测试。

### P0：VLM 成本与输出治理

- [ ] 增加 `--ab-mode full|needed|skip`。
- [ ] 增加 `--output-level compact|full|debug`。
- [ ] `compact` 只写 `baseline_result.json` 和批跑汇总。
- [ ] 建立共享模型缓存，key 使用模型名、提示词类型、图片哈希和动作/checkpoint 文本。
- [ ] 避免两次完整调用 `build_state_sequence`，复用视觉证据。
- [ ] 将参数接入 `start.sh`。
- [ ] 增加批跑断点续跑，跳过已有且 schema/version 一致的任务。

### P1：判定可靠性校准

- [ ] 使用标注集校准视觉边界权重和状态切分阈值。
- [ ] 校准重复动作的坐标、文本、页面相似度和连续次数阈值。
- [ ] 对 VLM confidence 做分桶准确率统计，不能直接把模型自评分当真实概率。
- [ ] 用数据替换规划失效中的固定置信度。
- [ ] 分别报告规则证据、模型证据和最终融合置信度。

### P1：强化规划失效

- [ ] 让 `src/evaluator/planning_failure.py` 真正消费 `state_sequence`。
- [ ] 区分“有意图但页面无变化”“进入错误状态”“任务目标根本不匹配”。
- [ ] 实现高置信 `objective_or_plan_mismatch`。
- [ ] 合并或废弃重复的 `src/common/planning_failure_detector.py`。
- [ ] 增加 subtype 混淆矩阵和首错步骤准确率。

### P2：接入后续能力

- [ ] 将 `src/efficiency/analyzer.py` 的 `darwin_result` 改为新链的 AB、state、verification 输入。
- [ ] 将 `src/trajectory/differential_judger.py` 改为消费新链结果。
- [ ] 在 `baseline_result.json` 增加 `efficiency_prediction` 和 `trajectory_deviation`。
- [ ] 最后再扩展环境中断、错误恢复和错误级联等异常类型。

### P2：工程与文档治理

- [ ] 为 legacy oracle 测试增加 pytest marker，默认测试不收集资源型 quick test。
- [ ] 增加可选真实 VLM 服务集成测试，默认单元测试继续使用 mock。
- [ ] 为 payload、baseline 和 batch result 增加 schema version 校验。
- [ ] 精简预处理输出策略，明确 `_deduped.json`、`_stategraph.json` 是否保留。
- [ ] 每轮架构变更同步更新主技术方案和专题文档。

## 7. 当前验证状态

新架构相关测试：

```text
105 passed
```

完整测试：

```text
110 passed, 5 failed
```

5 个失败全部来自 `src/oracle/quick_test.py` 缺少旧截图资源，不属于新主链功能回归。

## 8. 推荐实施顺序

```text
评测闭环
  -> 通用化 checkpoint 拆解
  -> VLM 成本与输出治理
  -> 阈值与置信度校准
  -> 强化规划失效
  -> 接入效率和轨迹差分模块
  -> 扩展更多异常类型
```
